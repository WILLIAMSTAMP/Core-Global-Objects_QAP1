1. Install node modules and dependencies by accessing terminal from "Weather" folder. 

2. From the command prompt of the terminal type :  < npm i weather-js > 

3. To run the program, inside the terminal type:  node Weather 

4. To update location and degree type change the search criteria of the search property inside "Weather.js"..

Example:
                          
                          Location             Degree Type
                             ↓                      ↓
weather.find({search: 'St. johns, NL', degreeType: 'C'}, function(err, result) {
  if(err) console.log(err);
 

 


